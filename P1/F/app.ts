// PROYECTO REACT CON TS

// Ahora procederemos a crear un proyecto, lo que haremos será para esta sección trabajar con Vite, dicho esto ejecutaremos 'npm create vite@latest', después creamos el nombre del proyecto y el paquete (se pueden poner el mismo nombre), después elegimos para qué lo queremos (lo seteamos en React), después nos indica que coloquemos una variante, nosotros seteamos TypeScript + SWC (SWC es un compilador muy eficiente para JS que se puede usar ocn TS Superspeed Web Compiler).

// Una vez finalizados estos pasos, ya se ha creado el proyecto con éxito

// Eliminamos la carpeta de public, assets (react.svg), App.css, App.tsx, index.css

// eliminamos contenido del main.tsx

// Una vez finalizado esto, en el main.tsx al quitar el contenido indicado en ese archivo tendremos cosas en rojo, para que quede normal, tenemos que escribir 'npm install'

// Al finalizarse este proceso ya estamos listos para continuar


// UNA VEZ FINALIZADO EL PROYECTO...

// Se puede suibir al internet, sin embargo para subirlo lo que se tiene que hacer es escribir en la consola 'npm run build'; de esta manera se genera la carpeta dist, y de esta manera se genera un html, un css y un js a pesar de haber utilizado ts; una vez finalizado solo se arrastra esta carpeta para donde se quiera subir y estaría listo.